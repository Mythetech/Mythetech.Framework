@using MudBlazor
@using Microsoft.AspNetCore.Components.Routing
@using Mythetech.Framework.Components.Buttons
@using Mythetech.Framework.Utilities
@inject NavigationManager Nav
@implements IDisposable

<div class="context-panel-item-wrapper" data-panel-id="@Id" @ondblclick="HandleDoubleClick">
    @if (IsActive)
    {
        <MudIconButton Class="@ActiveClass"
                       Color="Color.Primary"
                       Icon="@Icon"
                       OnClick="HandleClick" />
    }
    else
    {
        <MtIconButton Color="Color.Surface"
                    Icon="@Icon"
                    OnClick="HandleClick"
                    Tooltip="@Title"
                    TooltipPlacement="Placement.Right" />
    }
    @if (BadgeColor.HasValue)
    {
        <span class="@GetBadgeClass()"></span>
    }
</div>

@code {
    /// <summary>
    /// Unique identifier for this panel.
    /// </summary>
    [Parameter, EditorRequired]
    public string Id { get; set; } = string.Empty;

    /// <summary>
    /// MudBlazor icon to display in the icon bar.
    /// </summary>
    [Parameter, EditorRequired]
    public string Icon { get; set; } = string.Empty;

    /// <summary>
    /// Title shown in tooltip and panel header.
    /// </summary>
    [Parameter, EditorRequired]
    public string Title { get; set; } = string.Empty;

    /// <summary>
    /// Content to render when this panel is active.
    /// </summary>
    [Parameter]
    public RenderFragment? PanelContent { get; set; }

    /// <summary>
    /// Optional badge color for status indication (e.g., git changes, connection status).
    /// </summary>
    [Parameter]
    public Color? BadgeColor { get; set; }

    /// <summary>
    /// Display order (lower values appear first). Default 100.
    /// </summary>
    [Parameter]
    public int Order { get; set; } = 100;

    /// <summary>
    /// The ID of the currently active panel (cascaded from parent).
    /// </summary>
    [CascadingParameter(Name = "ActivePanelId")]
    public string? ActivePanelId { get; set; }

    /// <summary>
    /// Callback to register this item with the parent drawer (cascaded from parent).
    /// </summary>
    [CascadingParameter(Name = "OnPanelItemClicked")]
    public EventCallback<ContextPanelItem> OnPanelItemClicked { get; set; }

    /// <summary>
    /// Callback raised when this item is double clicked (cascaded from parent).
    /// </summary>
    [CascadingParameter(Name = "OnPanelItemDoubleClicked")]
    public EventCallback<ContextPanelItem> OnPanelItemDoubleClicked { get; set; }

    /// <summary>
    /// Callback to register this item with the parent drawer.
    /// </summary>
    [CascadingParameter(Name = "RegisterPanelItem")]
    public Action<ContextPanelItem>? RegisterPanelItem { get; set; }

    /// <summary>
    /// Optional route prefix for route-based highlighting.
    /// When set, the icon highlights if the current URI starts with this prefix,
    /// even when the panel is closed.
    /// </summary>
    [Parameter]
    public string? RoutePrefix { get; set; }

    /// <summary>
    /// When true, route-based highlighting matches only the <see cref="RoutePrefix"/> route itself,
    /// not the routes nested beneath it. Double clicking still navigates to <see cref="RoutePrefix"/>.
    /// Use it for an index page whose child pages have panel items of their own, so both icons do not
    /// light up at once.
    /// </summary>
    [Parameter]
    public bool ExactRouteMatch { get; set; }

    /// <summary>
    /// CSS class applied when the item is active due to route matching.
    /// </summary>
    [Parameter]
    public string ActiveRouteClass { get; set; } = "context-item-active";

    /// <summary>
    /// CSS class applied when the item is active due to the panel being open.
    /// </summary>
    [Parameter]
    public string ActivePanelClass { get; set; } = "context-item-active";

    /// <summary>
    /// Whether this panel is currently active (panel open or route match).
    /// </summary>
    public bool IsActive => IsPanelActive || IsRouteActive;

    private string ActiveClass => Css.MergeIf(
        (ActiveRouteClass, IsRouteActive),
        (ActivePanelClass, IsPanelActive)
    );

    private bool IsPanelActive => !string.IsNullOrEmpty(ActivePanelId) && ActivePanelId == Id;

    private bool IsRouteActive => !string.IsNullOrEmpty(RoutePrefix)
        && (ExactRouteMatch
            ? IsOnExactRoute
            : Nav.ToBaseRelativePath(Nav.Uri).StartsWith(RoutePrefix.TrimStart('/'), StringComparison.OrdinalIgnoreCase));

    /// <summary>
    /// Whether the current route is this panel's route exactly, rather than a page nested beneath it.
    /// A nested page still counts as somewhere to navigate away from.
    /// </summary>
    internal bool IsOnExactRoute => !string.IsNullOrEmpty(RoutePrefix)
        && string.Equals(CurrentPath(), RoutePrefix.Trim('/'), StringComparison.OrdinalIgnoreCase);

    private string CurrentPath()
    {
        var path = Nav.ToBaseRelativePath(Nav.Uri);
        var end = path.IndexOfAny(['?', '#']);
        return (end >= 0 ? path[..end] : path).Trim('/');
    }

    protected override void OnInitialized()
    {
        RegisterPanelItem?.Invoke(this);

        if (!string.IsNullOrEmpty(RoutePrefix))
        {
            Nav.LocationChanged += OnLocationChanged;
        }
    }

    private void OnLocationChanged(object? sender, LocationChangedEventArgs e)
    {
        InvokeAsync(StateHasChanged);
    }

    public void Dispose()
    {
        Nav.LocationChanged -= OnLocationChanged;
    }

    private async Task HandleClick()
    {
        await OnPanelItemClicked.InvokeAsync(this);
    }

    private async Task HandleDoubleClick()
    {
        await OnPanelItemDoubleClicked.InvokeAsync(this);
    }

    private string GetBadgeClass()
    {
        var baseClass = "context-panel-badge";
        return BadgeColor switch
        {
            Color.Success => $"{baseClass} context-panel-badge--success",
            Color.Warning => $"{baseClass} context-panel-badge--warning",
            Color.Error => $"{baseClass} context-panel-badge--error",
            Color.Info => $"{baseClass} context-panel-badge--info",
            Color.Primary => $"{baseClass} context-panel-badge--primary",
            Color.Secondary => $"{baseClass} context-panel-badge--secondary",
            _ => baseClass
        };
    }
}
